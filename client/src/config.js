const config = {
    // Use relative URL for production/deployment - this will use the same host as the frontend
    API_URL: "/api/",
    // API_URL: "http://localhost:7010/api/", // Only for development
};

export default config;
